{
	"name": "microsevicessimulation",
	"version": "1.0.0",
	"description": "",
	"main": "fetching.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"reinstall": "rm -rf node_modules && rm package-lock.json && npm install"
	},
	"private": true,
	"dependencies": {
		"axios": "^1.7.9",
		"express": "^4.21.1",
		"open-lineage-client": "^1.0.5",
		"uuid": "^11.0.3"
	}
}
